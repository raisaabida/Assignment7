# React + Vite

1. What is JSX, and why is it used?
Ans: It is a simple extension for javascript which works like html.It is used because it makes codes look easier to read as html and javascript both work together in this case.


2. What is the difference between State and Props?
Ans: State defines inside the components itself whereas Props define inside the parent component.State can change anything inside code but Props can't.
3. What is the useState hook, and how does it work?
4. How can you share state between components in React?
5. How is event handling done in React?